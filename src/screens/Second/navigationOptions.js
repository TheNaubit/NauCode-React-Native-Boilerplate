import {t} from '../../i18n';

const options = {
  topBar: {
    title: {
      text: t('second_screen_title'),
      color: 'white',
    },
    background: {
      color: 'red',
    },
  },
};

export default options;
